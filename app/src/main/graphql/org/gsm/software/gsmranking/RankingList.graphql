query RankingResult($criteria: String!, $count: Int!, $page: Int!, $generation: Int!) {
    ranking(criteria: $criteria, count: $count, page: $page, generation: $generation) {
        contributions
        name
        generation
        nickname
        repos_url
        avatar_url
        stared
        forked
    }
}
